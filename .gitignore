# OS / Editor
.DS_Store
Thumbs.db
.vscode/*.code-workspace

# Secrets / local config
.vscode/sftp.json

# Logs and temp artifacts
logs/
tmp_*/
*.log
*.tmp

# Dependencies / build caches (if ever added)
node_modules/
dist/
build/
.cache/

# Python cache (for utility scripts)
__pycache__/
*.pyc

# Sofa recolor tool — generated previews (keep out of SCM; avoids UI freeze on big PNGs)
sofa-recolor-tool/output/

# Local manufacturer PDFs for diagram export tool (see tools/sectional-diagrams/)
tools/sectional-diagrams/pdfs/*.pdf
tools/sectional-diagrams/thumbs/
