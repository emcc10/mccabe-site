name: Deploy to Volusion

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Clean apt sources
        run: |
          sudo rm -f /etc/apt/sources.list.d/microsoft-prod.list
          sudo rm -f /etc/apt/sources.list.d/azure-cli.list
          sudo apt-get update

      - name: Install tools
        run: |
          sudo apt-get install -y lftp netcat-openbsd

      - name: Test SFTP connection
        run: |
          nc -vz "$FTP_SERVER" 2222
        env:
          FTP_SERVER: ${{ secrets.FTP_SERVER }}

      - name: Deploy files to Volusion
        run: |
          echo "deployed=$(date -u '+%Y-%m-%dT%H:%M:%SZ') commit=$GITHUB_SHA" > deploy-marker.txt
          lftp -u "$FTP_USERNAME","$FTP_PASSWORD" sftp://"$FTP_SERVER":2222 <<'EOF' || true
          set sftp:auto-confirm yes
          set net:max-retries 2
          set net:timeout 30
          set cmd:fail-exit no

          echo "Uploading deploy marker"
          put deploy-marker.txt -o /vspfiles/js/deploy-marker.txt

          echo "Uploading template to /template_266.html"
          put template_266.html -o /template_266.html

          echo "Uploading template to /v/template_266.html"
          put template_266.html -o /v/template_266.html

          echo "Uploading custom-safe.css"
          put vspfiles/css/custom-safe.css -o /vspfiles/css/custom-safe.css

          echo "Uploading sectional-configs.js"
          put vspfiles/js/sectional-configs.js -o /vspfiles/js/sectional-configs.js

          echo "Uploading mtl-sectional-renderer.js"
          put vspfiles/js/mtl-sectional-renderer.js -o /vspfiles/js/mtl-sectional-renderer.js

          bye
          EOF
        env:
          FTP_SERVER: ${{ secrets.FTP_SERVER }}
          FTP_USERNAME: ${{ secrets.FTP_USERNAME }}
          FTP_PASSWORD: ${{ secrets.FTP_PASSWORD }}

      - name: Retry locked files (template + renderer JS)
        shell: bash
        run: |
          set +e
          # Both template_266.html and mtl-sectional-renderer.js can be locked by Volusion's
          # runtime when first attempted. Retry each up to 6 times with increasing sleeps.
          # Each attempt is its own lftp session so the server has time to release file handles.

          retry_file () {
            local LOCAL="$1"
            local REMOTE_A="$2"
            local REMOTE_B="$3"
            local LABEL="$4"
            local attempt
            for attempt in 1 2 3 4 5 6; do
              echo "=== [$LABEL] retry attempt $attempt ==="
              sleep $((attempt * 5))
              local CMD="set sftp:auto-confirm yes; set net:max-retries 2; set net:timeout 30; put $LOCAL -o $REMOTE_A;"
              if [ -n "$REMOTE_B" ]; then
                CMD="$CMD put $LOCAL -o $REMOTE_B;"
              fi
              CMD="$CMD bye"
              OUT=$(lftp -u "$FTP_USERNAME","$FTP_PASSWORD" sftp://"$FTP_SERVER":2222 -e "$CMD" 2>&1)
              echo "$OUT"
              echo "$OUT" | grep -q "Access failed"
              STILL_LOCKED=$?
              if [ "$STILL_LOCKED" != "0" ]; then
                echo "=== [$LABEL] uploaded successfully on attempt $attempt ==="
                return 0
              fi
              if [ "$attempt" = "6" ]; then
                echo "=== WARNING: [$LABEL] still locked after 6 attempts. Continuing. ==="
              fi
            done
            return 0
          }

          retry_file "template_266.html" "/template_266.html" "/v/template_266.html" "template_266.html"
          retry_file "vspfiles/js/mtl-sectional-renderer.js" "/vspfiles/js/mtl-sectional-renderer.js" "" "mtl-sectional-renderer.js"
          exit 0
        env:
          FTP_SERVER: ${{ secrets.FTP_SERVER }}
          FTP_USERNAME: ${{ secrets.FTP_USERNAME }}
          FTP_PASSWORD: ${{ secrets.FTP_PASSWORD }}

      - name: Deploy summary
        run: |
          echo "Volusion deploy complete."
          echo "Uploaded:"
          echo "- /template_266.html"
          echo "- /v/template_266.html"
          echo "- /vspfiles/css/custom-safe.css"
          echo "- /vspfiles/js/sectional-configs.js"
          echo "- /vspfiles/js/mtl-sectional-renderer.js"
          echo "- /vspfiles/js/deploy-marker.txt  <- verify at https://www.mccabestheaterandliving.com/v/vspfiles/js/deploy-marker.txt"
