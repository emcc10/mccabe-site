name: Deploy to Volusion

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Remove problematic Microsoft/Azure apt sources
        run: |
          sudo rm -f /etc/apt/sources.list.d/microsoft-prod.list
          sudo rm -f /etc/apt/sources.list.d/azure-cli.list
          sudo apt-get update

      - name: Install lftp and netcat
        run: |
          sudo apt-get install -y lftp netcat-openbsd openssh-client

      - name: Test SFTP connection
        run: |
          nc -vz "$FTP_SERVER" 2222
        env:
          FTP_SERVER: ${{ secrets.FTP_SERVER }}

      - name: Deploy template, JS, and CSS to Volusion
        env:
          FTP_SERVER: ${{ secrets.FTP_SERVER }}
          FTP_USERNAME: ${{ secrets.FTP_USERNAME }}
          FTP_PASSWORD: ${{ secrets.FTP_PASSWORD }}
        run: bash .github/scripts/deploy-volusion.sh

      - name: Print deploy marker
        run: |
          STAMP="$(date -u '+%Y%m%d%H%M')"
          echo "Deploy completed at $(date -u)"
          echo "Uploaded template (both roots), JS, custom-safe.css, mc-live-patch.css, mccabe-overrides.css"
          echo "After deploy, View Source on any page should contain: MC_LIVE_PATCH_20260518"
          echo "Verify new patch file (not cached under old name):"
          echo "https://www.mccabestheaterandliving.com/v/vspfiles/css/mc-live-patch.css?v=20260518"
          echo "Verify renderer (CDN path; SFTP uploads to /vspfiles/js/..., not /v/vspfiles/js):"
          echo "https://www.mccabestheaterandliving.com/v/vspfiles/js/mtl-sectional-renderer.js?v=deploy-test-${STAMP}"
