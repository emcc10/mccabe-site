name: Find Real Volusion Template Path

on:
  workflow_dispatch:

jobs:
  find-template-path:
    runs-on: ubuntu-latest

    steps:
<<<<<<< HEAD
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Show GitHub file being deployed
        run: |
          echo "Current branch/ref:"
          echo "$GITHUB_REF"

          echo "Current commit:"
          git log -1 --oneline

          echo "Repo root:"
          pwd

          echo "Confirm template file exists:"
          ls -la template_266.html

          echo "Confirm CSS / overrides exist:"
          ls -la vspfiles/css/custom-safe.css vspfiles/templates/266/css/mccabe-overrides.css

          echo "Local template marker search:"
          grep -n "MC_DEPLOY_VERIFY\|mc-deploy-verify" template_266.html || true

          echo "First 20 lines of local template:"
          head -20 template_266.html

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install Paramiko
        run: pip install --disable-pip-version-check "paramiko>=3.4,<4"
=======
      - name: Install deploy tools
        run: |
          sudo apt-get update
          sudo apt-get install -y lftp netcat-openbsd
>>>>>>> 50467bc206abf09a83cb9dd9765d5e6fc7c61ca6

      - name: Check deploy secrets
        env:
          SFTP_HOST: ${{ secrets.FTP_SERVER }}
          SFTP_USER: ${{ secrets.FTP_USERNAME }}
          SFTP_PASS: ${{ secrets.FTP_PASSWORD }}
        run: |
          test -n "$SFTP_HOST" || (echo "ERROR: GitHub secret FTP_SERVER is blank" && exit 1)
          test -n "$SFTP_USER" || (echo "ERROR: GitHub secret FTP_USERNAME is blank" && exit 1)
          test -n "$SFTP_PASS" || (echo "ERROR: GitHub secret FTP_PASSWORD is blank" && exit 1)

          echo "FTP_SERVER / FTP_USERNAME / FTP_PASSWORD are set (mapped to SFTP_* for scripts)."

<<<<<<< HEAD
      - name: Test Volusion SFTP port
=======
      - name: Test SFTP
>>>>>>> 50467bc206abf09a83cb9dd9765d5e6fc7c61ca6
        env:
          SFTP_HOST: ${{ secrets.FTP_SERVER }}
          SFTP_PORT: ${{ secrets.SFTP_PORT }}
        run: |
          python3 <<'PY'
          import os
          import socket

<<<<<<< HEAD
          host = os.environ["SFTP_HOST"]
          port = int(os.environ.get("SFTP_PORT") or "2222")
          s = socket.socket()
          s.settimeout(25)
          try:
              s.connect((host, port))
          finally:
              s.close()
          print(f"OK: TCP connect to {host}:{port}")
          PY

      - name: Upload template + CSS + theme overrides (Paramiko)
        env:
          SFTP_HOST: ${{ secrets.FTP_SERVER }}
          SFTP_USER: ${{ secrets.FTP_USERNAME }}
          SFTP_PASS: ${{ secrets.FTP_PASSWORD }}
          SFTP_PORT: ${{ secrets.SFTP_PORT }}
          SFTP_TEMPLATE_REMOTE: ${{ secrets.SFTP_TEMPLATE_REMOTE }}
          SFTP_CSS_REMOTE_FILE: ${{ secrets.SFTP_CSS_REMOTE_FILE }}
        run: python scripts/volusion_sftp_deploy.py

      - name: Verify SFTP + live template marker
        env:
          SFTP_HOST: ${{ secrets.FTP_SERVER }}
          SFTP_USER: ${{ secrets.FTP_USERNAME }}
          SFTP_PASS: ${{ secrets.FTP_PASSWORD }}
          SFTP_PORT: ${{ secrets.SFTP_PORT }}
          SFTP_TEMPLATE_REMOTE: ${{ secrets.SFTP_TEMPLATE_REMOTE }}
        run: python scripts/volusion_sftp_verify_template.py
=======
      - name: Search Volusion files for manual marker
        env:
          FTP_SERVER: ${{ secrets.FTP_SERVER }}
          FTP_USERNAME: ${{ secrets.FTP_USERNAME }}
          FTP_PASSWORD: ${{ secrets.FTP_PASSWORD }}
        run: |
          lftp <<EOF
          set sftp:auto-confirm yes
          set ssl:verify-certificate no
          set net:timeout 120
          set net:max-retries 2
          set net:reconnect-interval-base 10
          set xfer:clobber yes
          set cmd:fail-exit no

          open -u "$FTP_USERNAME","$FTP_PASSWORD" sftp://"$FTP_SERVER":2222

          echo "Downloading likely root/template files..."

          get /template_266.html -o template-root-266.html
          get /template.html -o template-root.html
          get /v/template_266.html -o template-v-266.html
          get /v/template.html -o template-v.html

          get /vspfiles/template_266.html -o template-vspfiles-266.html
          get /vspfiles/template.html -o template-vspfiles.html

          get /vspfiles/templates/266/template_266.html -o template-vspfiles-templates-266.html
          get /vspfiles/templates/266/template.html -o template-vspfiles-templates-template.html
          get /vspfiles/templates/266/template.asp -o template-vspfiles-templates-asp.html
          get /vspfiles/templates/266/template.htm -o template-vspfiles-templates-htm.html

          get /v/vspfiles/templates/266/template_266.html -o template-v-vspfiles-templates-266.html
          get /v/vspfiles/templates/266/template.html -o template-v-vspfiles-templates-template.html
          get /v/vspfiles/templates/266/template.asp -o template-v-vspfiles-templates-asp.html
          get /v/vspfiles/templates/266/template.htm -o template-v-vspfiles-templates-htm.html

          bye
          EOF

          echo "Downloaded files:"
          ls -la template-* || true

          echo "Searching for Volusion editor manual marker..."
          grep -Rni "VOLUSION_EDITOR_MANUAL_MARKER_20260429" template-* || true

          echo "Searching for anything that looks like your current template..."
          grep -Rni "mccabe-overrides\|custom-safe\|mc-mccabe-overrides-body-last\|MC_DEPLOY_VERIFY\|template_266" template-* | head -100 || true
>>>>>>> 50467bc206abf09a83cb9dd9765d5e6fc7c61ca6
