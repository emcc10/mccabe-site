# SAR-MNKY-STR-XL-LG Color Options

Applies existing global Color option IDs (same as SAR-MNKY-STR) to the XL product.

1. Import `Options_Apply.csv` (Options) OR `Products_Import.csv` (Products OptionIDs).
2. Variant images: `{CODE}-{optionId}-S.jpg` / `-T.jpg` (uploaded via SFTP).
