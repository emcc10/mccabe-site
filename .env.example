# Copy to .env.local and fill in ONLY the line below (do not copy this filename as the key).
HERO_GENERATIVE_PROVIDER=openai
OPENAI_API_KEY=

# After pasting your real sk-proj-... secret (150+ chars), run:
#   npm run hero:check-key
#   npm run render:hero-pipeline
